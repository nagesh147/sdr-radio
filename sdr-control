#!/usr/bin/env bash
# Prefer project venv (faster-whisper etc.), fall back to system python3
DIR="${SDR_DIR:-$HOME/SDR-Tools}"
if [ -x "$DIR/.venv/bin/python" ]; then
  exec "$DIR/.venv/bin/python" "$DIR/sdr-control-ui.py" "$@"
fi
exec python3 "$DIR/sdr-control-ui.py" "$@"
